import { Bot } from "lucide-react";

export default function AIChatbot() {
  return (
    <div className="fixed bottom-5 right-5 bg-primary text-white p-4 rounded-full shadow-lg cursor-pointer">
      <Bot size={30} />
    </div>
  );
}
